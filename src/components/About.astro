---
interface Props {
  title?: string;
  description?: string[];
}

const { 
  title = "Sobre Mí",
  description = [
    "Soy un desarrollador web apasionado por crear experiencias digitales excepcionales.",
    "Con experiencia en tecnologías frontend y backend, me enfoco en construir aplicaciones web modernas, rápidas y accesibles.",
    "Me gusta aprender constantemente nuevas tecnologías y enfrentar desafíos que me permitan crecer profesionalmente."
  ]
} = Astro.props;
---

<section id="sobre-mi" class="relative py-24 overflow-hidden">
  <!-- Fondo con degradado sutil -->
  <div class="absolute inset-0 bg-gradient-to-b from-black to-blue-950 z-0">
    <!-- Grid futurista de fondo -->
    <div class="absolute inset-0 opacity-10 bg-[linear-gradient(rgba(56,189,248,0.1)_1px,transparent_1px),linear-gradient(to_right,rgba(56,189,248,0.1)_1px,transparent_1px)] bg-[size:64px_64px]"></div>
  </div>
  
  <div class="container mx-auto px-6 relative z-10">
    <div class="text-center mb-16">
      <div class="inline-block mb-3">
        <span class="px-3 py-1 bg-blue-900/30 text-blue-400 rounded-full text-xs font-medium border border-blue-800/30 backdrop-blur-sm">
          CONÓCEME
        </span>
      </div>
      <h2 class="text-4xl font-bold text-white mb-3">{title}</h2>
      <div class="w-24 h-1 bg-gradient-to-r from-blue-500 to-purple-600 mx-auto rounded-full"></div>
    </div>
    
    <div class="max-w-4xl mx-auto">
      <div class="flex flex-col md:flex-row gap-12 items-center backdrop-blur-sm bg-white/5 rounded-2xl border border-white/10 p-6 md:p-8 shadow-xl">
        <div class="md:w-2/5 w-full">
          <!-- Efecto de marco futurista para la imagen -->
          <div class="relative mx-auto max-w-sm">
            <!-- Marco decorativo -->
            <div class="absolute inset-0 border-2 border-blue-500/30 rounded-2xl transform rotate-3"></div>
            <div class="absolute inset-0 border-2 border-purple-500/30 rounded-2xl transform -rotate-3"></div>
            
            <!-- Imagen principal con efecto neón sutil -->
            <div class="relative z-10 bg-gradient-to-br from-blue-900 to-purple-900 p-1 rounded-2xl shadow-lg overflow-hidden">
              <div class="aspect-square rounded-xl overflow-hidden bg-gradient-to-br from-blue-700 to-purple-800 flex items-center justify-center shadow-inner">
                <!-- Puedes cambiar por una imagen real -->
                <span class="text-8xl">😊</span>
              </div>
              
              <!-- Efectos de brillo -->
              <div class="absolute -top-10 -right-10 w-20 h-20 bg-blue-500/20 blur-xl rounded-full"></div>
              <div class="absolute -bottom-10 -left-10 w-24 h-24 bg-purple-500/20 blur-xl rounded-full"></div>
            </div>
          </div>
        </div>
        
        <div class="md:w-3/5 w-full text-gray-300">
          <div class="space-y-4">
            {description.map((paragraph) => (
              <p class="text-lg leading-relaxed">{paragraph}</p>
            ))}
          </div>
          
          <div class="mt-8 flex flex-wrap gap-3">
            <!-- Etiquetas de intereses o características -->
            <span class="px-4 py-2 rounded-full bg-blue-500/10 text-blue-300 text-sm border border-blue-500/20">Diseño UI/UX</span>
            <span class="px-4 py-2 rounded-full bg-purple-500/10 text-purple-300 text-sm border border-purple-500/20">Desarrollo Web</span>
            <span class="px-4 py-2 rounded-full bg-blue-500/10 text-blue-300 text-sm border border-blue-500/20">Innovación</span>
            <span class="px-4 py-2 rounded-full bg-purple-500/10 text-purple-300 text-sm border border-purple-500/20">Tecnología</span>
          </div>
          
          <div class="mt-8">
            <a href="#contacto" class="group relative inline-flex items-center justify-center px-6 py-3 overflow-hidden font-medium rounded-lg text-white shadow-md">
              <span class="absolute inset-0 w-full h-full bg-gradient-to-br from-blue-600 to-purple-600 group-hover:from-purple-600 group-hover:to-blue-600 transition-all duration-300"></span>
              <span class="relative flex items-center">
                Contáctame
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2 transform group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3" />
                </svg>
              </span>
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section> 