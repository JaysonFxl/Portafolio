---
interface ContactLink {
  platform: string;
  url: string;
  icon?: string;
}

interface Props {
  title?: string;
  email?: string;
  location?: string;
  socialLinks?: ContactLink[];
}

const { 
  title = "Contacto",
  email = "fseba196@gmail.com",
  location = "Mulchen, Chile",
  socialLinks = [
    {
      platform: "LinkedIn",
      url: "https://linkedin.com/in/jayson-flores-cartes-7b2536249/",
      icon: "linkedin"
    },
    {
      platform: "GitHub",
      url: "https://github.com/JaysonFxl",
      icon: "github"
    },
    {
      platform: "Twitter",
      url: "https://twitter.com/tuusuario",
      icon: "twitter"
    }
  ]
} = Astro.props;
---

<section id="contacto" class="relative py-24 overflow-hidden">
  <!-- Fondo con degradado futurista -->
  <div class="absolute inset-0 bg-gradient-to-b from-blue-950 to-black z-0">
    <!-- Partículas/puntos de fondo -->
    <div class="absolute inset-0 opacity-30 bg-[radial-gradient(circle_at_center,rgba(56,189,248,0.15)_0.5px,transparent_0.5px)] bg-[size:24px_24px]"></div>
    
    <!-- Formas flotantes decorativas -->
    <div class="absolute top-20 left-10 w-72 h-72 bg-blue-600/5 rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 right-10 w-80 h-80 bg-purple-600/5 rounded-full blur-3xl"></div>
  </div>
  
  <div class="container mx-auto px-6 relative z-10">
    <div class="text-center mb-16">
      <div class="inline-block mb-3">
        <span class="px-3 py-1 bg-blue-900/30 text-blue-400 rounded-full text-xs font-medium border border-blue-800/30 backdrop-blur-sm">
          CONECTEMOS
        </span>
      </div>
      <h2 class="text-4xl font-bold text-white mb-3">{title}</h2>
      <div class="w-24 h-1 bg-gradient-to-r from-blue-500 to-purple-600 mx-auto rounded-full"></div>
      <p class="mt-4 max-w-2xl mx-auto text-blue-100/70">Estoy disponible para nuevos proyectos y colaboraciones. ¡Hablemos sobre tu próxima idea!</p>
    </div>
    
    <div class="max-w-5xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12">
      <div class="backdrop-blur-sm bg-white/5 rounded-2xl border border-white/10 p-8 hover:shadow-blue-500/5 transition-all duration-300 hover:border-blue-500/20">
        <h3 class="text-2xl font-semibold mb-6 text-white">Hablemos</h3>
        <p class="mb-8 text-blue-100/70 leading-relaxed">
          ¿Tienes un proyecto en mente o simplemente quieres decir hola? No dudes en contactarme para discutir cómo puedo ayudarte a materializar tus ideas.
        </p>
        
        <div class="space-y-6">
          <div class="flex items-center group">
            <div class="w-12 h-12 rounded-full bg-blue-900/50 flex items-center justify-center mr-4 border border-blue-500/20 backdrop-blur-sm group-hover:bg-blue-800/50 transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
              </svg>
            </div>
            <a href={`mailto:${email}`} class="text-blue-300 hover:text-blue-200 transition-colors group-hover:translate-x-1 transform transition-transform duration-300">{email}</a>
          </div>
          
          <div class="flex items-center group">
            <div class="w-12 h-12 rounded-full bg-blue-900/50 flex items-center justify-center mr-4 border border-blue-500/20 backdrop-blur-sm group-hover:bg-blue-800/50 transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
            </div>
            <span class="text-blue-300 group-hover:translate-x-1 transform transition-transform duration-300">{location}</span>
          </div>
        </div>
        
        <div class="mt-10">
          <h4 class="text-xl font-medium mb-4 text-white">Conéctate conmigo en:</h4>
          <div class="flex space-x-4">
            {socialLinks.map((link) => (
              <a 
                href={link.url} 
                target="_blank" 
                rel="noopener noreferrer"
                class="w-12 h-12 rounded-full bg-blue-900/50 flex items-center justify-center hover:bg-blue-800/50 transition-colors border border-blue-500/20 backdrop-blur-sm hover:scale-110 transform transition-transform duration-300"
                title={link.platform}
              >
                <span class="text-blue-400">{link.platform[0]}</span>
              </a>
            ))}
          </div>
        </div>
      </div>
      
      <div class="backdrop-blur-sm bg-white/5 rounded-2xl border border-white/10 p-8 hover:shadow-blue-500/5 transition-all duration-300 hover:border-blue-500/20">
        <h3 class="text-2xl font-semibold mb-6 text-white">Envíame un mensaje</h3>
        <form class="space-y-6">
          <div>
            <label class="block mb-2 text-blue-300" for="name">Nombre</label>
            <div class="relative">
              <input 
                type="text" 
                id="name" 
                class="w-full px-5 py-3 rounded-lg bg-blue-950/50 placeholder-blue-400/50 text-white border border-blue-800/50 focus:outline-none focus:border-blue-500/70 backdrop-blur-sm transition-colors"
                placeholder="Tu nombre aqui"
              />
              <div class="absolute left-0 -bottom-px h-px w-0 group-focus-within:w-full bg-gradient-to-r from-blue-500 to-purple-500 transition-all duration-300"></div>
            </div>
          </div>
          
          <div>
            <label class="block mb-2 text-blue-300" for="email">Email</label>
            <div class="relative">
              <input 
                type="email" 
                id="email" 
                class="w-full px-5 py-3 rounded-lg bg-blue-950/50 placeholder-blue-400/50 text-white border border-blue-800/50 focus:outline-none focus:border-blue-500/70 backdrop-blur-sm transition-colors"
                placeholder="email@email.com"
              />
            </div>
          </div>
          
          <div>
            <label class="block mb-2 text-blue-300" for="message">Mensaje</label>
            <div class="relative">
              <textarea 
                id="message" 
                rows="5" 
                class="w-full px-5 py-3 rounded-lg bg-blue-950/50 placeholder-blue-400/50 text-white border border-blue-800/50 focus:outline-none focus:border-blue-500/70 backdrop-blur-sm transition-colors resize-none"
                placeholder="¿Cómo puedo ayudarte?"
              ></textarea>
            </div>
          </div>
          
          <button 
            type="submit" 
            class="group relative inline-flex items-center justify-center px-8 py-3 overflow-hidden font-medium rounded-lg text-white w-full"
          >
            <span class="absolute inset-0 w-full h-full bg-gradient-to-br from-blue-600 to-purple-600 group-hover:from-purple-600 group-hover:to-blue-600 transition-all duration-500"></span>
            <span class="relative flex items-center justify-center">
              Enviar Mensaje
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2 transform group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </span>
          </button>
        </form>
      </div>
    </div>
  </div>
</section> 