---
// Puedes agregar props si es necesario
interface Props {
  name?: string;
  title?: string;
}

const { 
  name = "Jayson Flores", 
  title = "Desarrollador FullStack"
} = Astro.props;
---

<section id="inicio" class="relative min-h-screen flex items-center justify-center overflow-hidden py-20">
  <!-- Fondo con gradiente futurista -->
  <div class="absolute inset-0 bg-gradient-to-br from-black via-blue-950 to-black z-0">
    <!-- Efecto de grid futurista -->
    <div class="absolute inset-0 opacity-20 bg-[radial-gradient(circle_at_1px_1px,rgba(56,189,248,0.15)_1px,transparent_0)] bg-[size:40px_40px]"></div>
    
    <!-- Formas geométricas animadas -->
    <div class="absolute top-20 left-10 w-64 h-64 bg-blue-500/10 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute bottom-20 right-10 w-80 h-80 bg-purple-500/10 rounded-full blur-3xl animate-pulse" style="animation-delay: 1s;"></div>
  </div>
  
  <div class="container mx-auto px-6 flex flex-col md:flex-row items-center relative z-10">
    <div class="md:w-1/2 flex flex-col items-start justify-center md:pr-10">
      <div class="inline-block mb-4 px-4 py-1 rounded-full border border-blue-500/20 bg-blue-500/10 text-blue-400 text-sm backdrop-blur-sm">
        ¡Bienvenido a mi portafolio digital!
      </div>
      
      <h1 class="text-5xl sm:text-6xl font-bold mb-4 text-white">
        Hola, soy <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-600">{name}</span>
      </h1>
      
      <h2 class="text-2xl mb-6 text-blue-100/80">{title}</h2>
      
      <p class="text-lg mb-8 text-gray-300 max-w-xl leading-relaxed">
        Desarrollo soluciones web creativas y funcionales, con un enfoque en la experiencia del usuario y código limpio.
      </p>
      
      <div class="flex flex-wrap gap-4">
        <a href="#proyectos" class="relative px-6 py-3 overflow-hidden rounded-lg group">
          <span class="absolute inset-0 w-full h-full transition duration-300 bg-gradient-to-r from-blue-500 to-purple-600"></span>
          <span class="relative flex items-center justify-center text-white font-medium">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
              <path d="M11 3a1 1 0 100 2h2.586l-6.293 6.293a1 1 0 101.414 1.414L15 6.414V9a1 1 0 102 0V4a1 1 0 00-1-1h-5z" />
              <path d="M5 5a2 2 0 00-2 2v8a2 2 0 002 2h8a2 2 0 002-2v-3a1 1 0 10-2 0v3H5V7h3a1 1 0 000-2H5z" />
            </svg>
            Ver Proyectos
          </span>
        </a>
        
        <a href="#contacto" class="px-6 py-3 rounded-lg border border-blue-500/50 hover:bg-blue-500/10 transition duration-300 text-blue-300 flex items-center justify-center">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" viewBox="0 0 20 20" fill="currentColor">
            <path d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z" />
            <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z" />
          </svg>
          Contacto
        </a>
      </div>
      
      <!-- Indicadores de scroll -->
      <a href="#sobre-mi" class="hidden md:flex items-center mt-16 text-blue-300/60 hover:text-blue-400 transition-colors duration-300 cursor-pointer group">
        <span class="mr-2 text-sm">Descubre más</span>
        <div class="animate-bounce group-hover:animate-pulse">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M16.707 10.293a1 1 0 010 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 111.414-1.414L10 15.586l5.293-5.293a1 1 0 011.414 0z" clip-rule="evenodd" />
          </svg>
        </div>
      </a>
    </div>
    
    <div class="md:w-1/2 mt-10 md:mt-0 flex justify-center">
      <div class="relative">
        <!-- Círculos decorativos alrededor de la imagen -->
        <div class="absolute inset-0 border-2 border-dashed border-blue-500/30 rounded-full animate-spin-slow"></div>
        <div class="absolute inset-4 border border-purple-500/20 rounded-full"></div>
        
        <!-- Imagen o avatar del perfil -->
        <div class="w-64 h-64 bg-gradient-to-br from-blue-600 to-purple-600 rounded-full flex items-center justify-center shadow-lg z-10 relative overflow-hidden backdrop-blur-sm border border-white/10">
          <!-- Puedes reemplazar con una imagen real -->
          <div class="text-8xl">👨‍💻</div>
          
          <!-- Efecto de luz -->
          <div class="absolute -top-10 -right-10 w-20 h-20 bg-white/20 blur-xl rounded-full"></div>
        </div>
        
        <!-- Elementos decorativos flotantes -->
        <div class="absolute -top-4 -right-4 w-8 h-8 bg-blue-500 rounded-full blur-sm animate-float"></div>
        <div class="absolute -bottom-4 -left-4 w-6 h-6 bg-purple-500 rounded-full blur-sm animate-float" style="animation-delay: 1s;"></div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes spin-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }
  
  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }
  
  .animate-spin-slow {
    animation: spin-slow 20s linear infinite;
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style> 